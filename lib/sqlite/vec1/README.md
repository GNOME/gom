# SQLite Vec1

This directory vendors `vec1.c` from the SQLite Vec1 project:

https://sqlite.org/vec1

The copy currently comes from SQLite Vec1 trunk as fetched on 2026-05-26.
The source file declares version `0.5` via `VEC1_VERSION`.
